name: Hello word

on:
  push:
    branches:
      main
jobs:
  first helo world job:
    runs on:ubuntu latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
        run: echo "hello world GA testing"
